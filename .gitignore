# Local test photos. Real camera files with GPS in their EXIF: never version controlled.
test-photos/

# Rendered cards, which inherit whatever was in the photo they were made from.
outputs/

# Python
.venv/
__pycache__/
*.egg-info/
.pytest_cache/

# Scanned handwritten signature, private. Never commit it, wherever it lives.
signature.png
**/signature.png

# Rendered cards have the signature burned into the image, so they stay out too.
*.out.png
*.out.jpg
output/

# macOS
.DS_Store
